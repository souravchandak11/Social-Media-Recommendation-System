{
    "functions": {
        "api/**": {
            "excludeFiles": "{app,node_modules,data/raw,notebooks,results}/**"
        }
    },
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "api/main.py"
        }
    ]
}