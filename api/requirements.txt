# Minimal requirements for Vercel Serverless Functions
# This avoids the 250MB unzipped size limit by excluding heavy libraries like scikit-learn, streamlit, and plotly.

fastapi>=0.109.0
pandas>=2.2.0
numpy>=1.26.0
joblib>=1.3.0
pydantic>=2.0.0
